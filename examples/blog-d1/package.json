{
  "name": "@comp/example-blog-d1",
  "version": "0.0.0",
  "private": true,
  "description": "Reference Comp app: Cloudflare D1 + Drizzle",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.client.json --noEmit",
    "build:client": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.client.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply blog --local"
  },
  "dependencies": {
    "@comp/admin": "workspace:*",
    "@comp/auth": "workspace:*",
    "@comp/core": "workspace:*",
    "@comp/mcp": "workspace:*",
    "@comp/server": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.6.14",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "@types/react": "^19.0.1",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "drizzle-kit": "^0.30.1",
    "vite": "^6.0.5",
    "wrangler": "^3.99.0"
  }
}
